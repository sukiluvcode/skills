[project]
name = "sisyphus"
version = "0.2.0"
description = "Two-stage label→extract pipeline for structured data mining from scientific papers"
authors = [{ name = "soike lab", email = "luvsoike@icloud.com" }]
readme = "README.md"
requires-python = ">=3.10,<3.14"

dependencies = [
    "python-dotenv>=1.0.1",
    "tqdm>=4.66.2",
    "pandas>=2.2.2",
    "numpy>=1.26.4",

    # LLM / extraction stack
    "openai>=1.17.1",
    "tiktoken>=0.7.0",
    "langchain>=0.3.2",
    "langchain-openai>=0.2.2",
    "langchain-community>=0.3.1",
    "langchain-chroma>=0.1.4",
    "chromadb>=0.4.24",
    "dspy>=2.5.22",

    # Document loading
    "pypdf",
    "bs4>=0.0.2",
    "html5lib>=1.1,<2.0",
    "nltk>=3.8.1",

    # Storage
    "sqlmodel>=0.0.18",
    "aiosqlite>=0.20.0",
    "aiofiles>=23.2.1",

    # Utilities
    "pydantic>=2.0",
    "tenacity>=8.0",
    "scikit-learn>=1.4.2",
]

[project.optional-dependencies]
crawler = [
    "playwright>=1.43.0",
]
dev = [
    "ipykernel>=6.29.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "ipykernel>=6.29.4",
]
